<!DOCTYPE html>
<html lang="es">

<%- include('partials/head', { extraCSS: '<link rel="stylesheet" href="stylesheets/contactenos.css">' }) %>

    <body>
        <%- include('partials/header')%>
        <main>
            <section class="contacto">
                <h2>Contáctenos</h2>
                <p>¿Tienes alguna pregunta o sugerencia? ¡Déjanos un mensaje!</p>
        
                <% if (typeof error !== 'undefined') { %>
                    <p style="color: red;"><%= error %></p>
                <% } %>
        
                <% if (typeof success !== 'undefined') { %>
                    <p style="color: green;"><%= success %></p>
                <% } %>
        
                <form action="/contactenos" method="POST">
                    <label for="nombre">Nombre:</label>
                    <input type="text" id="nombre" name="nombre" required>
                    
                    <label for="email">Correo Electrónico:</label>
                    <input type="email" id="email" name="email" required>
                    
                    <label for="mensaje">Mensaje:</label>
                    <textarea id="mensaje" name="mensaje" rows="5" required></textarea>
                    
                    <button type="submit">Enviar</button>
                </form>
            </section>
        </main>
        
        <%- include('partials/footer')%>

    <script src="javascripts/menuToggle.js"></script>
</body>
</html>
